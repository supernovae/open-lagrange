# Planning Primitive

Open Lagrange turns vague goals into Planfiles. A Planfile is readable Markdown
with a strict executable YAML block. The control plane validates the structured
block before any runner uses it.
