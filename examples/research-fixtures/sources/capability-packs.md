# Capability Pack SDK Notes

Capability Packs expose bounded operations through descriptors, input schemas,
output schemas, risk levels, side-effect metadata, and approval requirements.

The SDK primitive layer provides safe building blocks for trusted pack
executors, including bounded HTTP, artifact writes, retry metadata, rate-limit
helpers, redaction, secrets by reference, approvals, and policy reports.
