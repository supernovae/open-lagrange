# Planning Primitive Notes

Open Lagrange represents intended work as Planfiles. A Planfile keeps readable
Markdown together with typed YAML execution data so a control plane can validate
shape, policy, approvals, and artifacts before work proceeds.

Planfiles are useful for research workflows because source selection,
extraction, briefing, and export can each be represented as bounded steps with
durable outputs.
