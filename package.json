{
  "name": "open-lagrange",
  "version": "0.1.0",
  "description": "Durable reconciliation around non-deterministic cognitive functions.",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build -w @open-lagrange/core && npm run build -w @open-lagrange/cli && npm run build -w @open-lagrange/web",
    "dev:worker": "npm run dev:worker -w @open-lagrange/core",
    "dev:web": "npm run dev -w @open-lagrange/web",
    "cli": "npm run cli -w @open-lagrange/cli --",
    "test": "vitest run",
    "typecheck": "npm run typecheck -w @open-lagrange/core && npm run build -w @open-lagrange/core && npm run typecheck -w @open-lagrange/cli && npm run typecheck -w @open-lagrange/web"
  },
  "keywords": [
    "reconciliation",
    "cognitive-pipeline",
    "runtime-boundary",
    "open-cot",
    "hatchet",
    "mcp"
  ],
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^25.6.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^3.1.0"
  }
}
